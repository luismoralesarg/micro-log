# Stripe Configuration (Backend - Secret Keys)
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxx

# Create a product in Stripe Dashboard, then get the Price ID
# https://dashboard.stripe.com/products → Create Product → Copy Price ID
STRIPE_PRICE_ID=price_xxx

# Webhook secret - Get this AFTER deploying and configuring webhook
# https://dashboard.stripe.com/webhooks → Add endpoint → Copy signing secret
STRIPE_WEBHOOK_SECRET=whsec_xxx
